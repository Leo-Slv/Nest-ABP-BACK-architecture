# Copie para .env e ajuste se necessário
NODE_ENV=development
PORT=3000

# PostgreSQL local (docker-compose na raiz: npm run db:up)
DATABASE_URL="postgresql://postgres:root@localhost:5432/crm_db?schema=public"
